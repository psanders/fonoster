WebUI module
============

To work in this modules run `mvn jetty:run`. The app will be available at *http://localhost:8080*.

For deployment to the standalone jetty-server copy the module *resources* in a folder named *static* and copy the file 
site.xml in the $jetty.base/webapps.